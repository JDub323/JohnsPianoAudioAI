import argparse

# make an override map for an apply overrides function, which takes in all of the command line args 
# and applies them to the configs
override_map = {

        }

# the function explained above
def apply_args_to_configs() -> None:
    return

# adds the arguments to the argparser which may be used by predict.py, train.py, or evaluate.py 
# this is to avoid repetition
def add_universal_args(parser: argparse.ArgumentParser) -> None:
    return
